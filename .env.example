# Copy this file to .env.local (gitignored, never committed) and fill in
# your own values, then restart `npm run dev`.

# Anthropic API key -- powers the "Generate with AI" recipe option.
# Get one at https://console.anthropic.com/settings/keys
# The app works fine without this; that one feature just won't be available.
ANTHROPIC_API_KEY=
